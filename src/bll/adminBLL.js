const bcrypt=require('bcryptjs');const jwt=require('jsonwebtoken');const dal=require('../dal/adminDAL');const SECRET=process.env.ADMIN_JWT_SECRET||process.env.JWT_SECRET||'WebBanDoCu_ADMIN_2026';
class B{async login(email,password){const a=await dal.findByEmail(email||'');if(!a||!a.IsActive||!(await bcrypt.compare(String(password||''),a.PasswordHash)))throw Error('Email hoặc mật khẩu quản trị không chính xác.');const token=jwt.sign({adminId:a.AdminUserId,email:a.Email,displayName:a.DisplayName,tokenType:'admin'},SECRET,{expiresIn:'8h'});return {token,user:{id:a.AdminUserId,email:a.Email,displayName:a.DisplayName,tokenType:'admin'}};}verifyToken(t){const p=jwt.verify(t,SECRET);if(p.tokenType!=='admin')throw Error('Token quản trị không hợp lệ.');return p;}getHomepageContent(){return dal.getHomepageContent();}saveHomepageContent(d){return dal.saveHomepageContent(d);}stats(){return dal.stats();}getOrders(){return dal.getOrders();}updateOrderStatus(id,s){if(!['Chờ xác nhận','Đã xác nhận','Đang giao','Đã giao','Đã hủy'].includes(s))throw Error('Trạng thái đơn không hợp lệ.');return dal.updateOrderStatus(id,s);}}
module.exports=new B();
